import { allowMethods,json,readJson } from '../_lib/http.mjs'
import { requireOrganization } from '../_lib/supabase-admin.mjs'
import { evolution,normalizePhone } from '../_lib/evolution.mjs'
export default async function handler(req,res){if(!allowMethods(req,res,['POST']))return;try{const {db,user,organizationId}=await requireOrganization(req);const input=await readJson(req);const body=String(input.body||'').trim();if(!body)throw Object.assign(new Error('Mensagem vazia.'),{status:400});const {data:conversation,error:ce}=await db.from('conversations').select('*').eq('id',input.conversationId).eq('organization_id',organizationId).single();if(ce)throw Object.assign(new Error('Conversa não encontrada.'),{status:404});const {data:connection,error:we}=await db.from('whatsapp_connections').select('*').eq('id',input.connectionId||conversation.whatsapp_connection_id).eq('organization_id',organizationId).eq('status','connected').single();if(we)throw Object.assign(new Error('WhatsApp não está conectado.'),{status:409});const {data:message,error:me}=await db.from('conversation_messages').insert({organization_id:organizationId,conversation_id:conversation.id,whatsapp_connection_id:connection.id,sender_user_id:user.id,direction:'outbound',body,status:'queued'}).select('*').single();if(me)throw me;try{const provider=await evolution(`/message/sendText/${encodeURIComponent(connection.instance_name)}`,{method:'POST',body:{number:normalizePhone(conversation.contact_phone),text:body}});const external=provider?.key?.id||provider?.message?.key?.id||null;await db.from('conversation_messages').update({status:'sent',sent_at:new Date().toISOString(),external_message_id:external,raw_payload:provider}).eq('id',message.id);await db.from('conversations').update({last_message_at:new Date().toISOString()}).eq('id',conversation.id);json(res,200,{message:{...message,status:'sent',external_message_id:external}})}catch(error){await db.from('conversation_messages').update({status:'failed',error_message:error.message}).eq('id',message.id);throw error}}catch(error){json(res,error.status||500,{error:error.message})}}
