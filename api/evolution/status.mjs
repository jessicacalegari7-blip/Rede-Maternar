import { allowMethods,json } from '../_lib/http.mjs'
import { requireOrganization } from '../_lib/supabase-admin.mjs'
import { evolution } from '../_lib/evolution.mjs'
export default async function handler(req,res){if(!allowMethods(req,res,['GET']))return;try{const {db,organizationId}=await requireOrganization(req);const id=String(req.query?.id||'');const {data:c,error}=await db.from('whatsapp_connections').select('*').eq('id',id).eq('organization_id',organizationId).single();if(error)throw Object.assign(new Error('Instância não encontrada.'),{status:404});const state=await evolution(`/instance/connectionState/${encodeURIComponent(c.instance_name)}`);const raw=state?.instance?.state||state?.state||'disconnected';const status=raw==='open'||raw==='connected'?'connected':raw==='connecting'?'connecting':'disconnected';await db.from('whatsapp_connections').update({status,last_seen_at:new Date().toISOString(),connected_at:status==='connected'?(c.connected_at||new Date().toISOString()):c.connected_at,last_error:null}).eq('id',c.id);json(res,200,{status,provider:state})}catch(error){json(res,error.status||500,{error:error.message})}}
